<p>
  <b>Title:</b>
  <%=h @event.title %>
</p>
<p>
Att: <%= @event.users.present.count %>/<%= @event.users.count %>
</p>
<table>
<% @event.users.each do |user| %>
<tr>
<td>
	<% if user.att == 1 then %>
	[◯]
	<% else %>
	[-]
	<% end %>
</td>
<td><%= link_to user.name, event_user_path(user.event_id, user.id) %></td>
<% end %>

</table>

</br>

</br>
<%= link_to 'Back', events_path %>
